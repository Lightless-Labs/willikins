disallowed-methods = [
    { path = "willikins_types::sink::SinkToken::new", reason = "SinkToken may only be constructed by the apply executor in willikins-core; tests must opt in with #[allow(clippy::disallowed_methods)]" },
    { path = "secrecy::ExposeSecret::expose_secret", reason = "a provider credential's bytes may only be read by Credential::authorize/authorize_header in willikins-providers-http, a secret domain type's bytes only by the derive's own generated `expose`, and an OpaqueSecret's bytes only by its own reveal_for_transform (the one token-less exception, for a pure transform/parse tool's Tool::read, which never receives a SinkToken); tests must opt in with #[allow(clippy::disallowed_methods)]" },
    { path = "secrecy::ExposeSecretMut::expose_secret_mut", reason = "secrecy 0.10's second way out of a SecretString (it returns &mut str); nothing in this workspace mutates a secret in place, so there is no allowed call site at all" },
]
