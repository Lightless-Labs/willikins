disallowed-methods = [
    { path = "willikins_types::sink::SinkToken::new", reason = "SinkToken may only be constructed by the apply executor in willikins-core; tests must opt in with #[allow(clippy::disallowed_methods)]" },
    { path = "secrecy::ExposeSecret::expose_secret", reason = "a provider credential's bytes may only be read by Credential::authorize in willikins-providers-http, and a secret domain type's bytes only by the derive's own generated `expose`; tests must opt in with #[allow(clippy::disallowed_methods)]" },
]
